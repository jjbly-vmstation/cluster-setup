---
# VMStation Bootstrap Secrets Template
# Copy to secrets.yml and secure with Ansible Vault before use

freeipa_admin_password: "CHANGEME"
keycloak_admin_user: "admin"
keycloak_admin_password: "CHANGEME"
keycloak_freeipa_ldap_url: "ldap://masternode.vmstation.local"
keycloak_freeipa_ldap_bind_dn: "uid=admin,cn=users,cn=accounts,dc=vmstation,dc=local"
keycloak_freeipa_ldap_bind_password: "CHANGEME"

# Optional: Ansible Vault password (not stored here, but referenced)
# vault_password_file: "~/.ansible/vault_pass.txt"

# Add any other bootstrap secrets here
# e.g., monitoring credentials, IPMI passwords, initial cluster tokens
monitoring_ipmi_password: "CHANGEME"
cluster_initial_token: "CHANGEME"

# Usage:
# 1. Copy this file to secrets.yml
# 2. Edit values securely
# 3. Encrypt with Ansible Vault: ansible-vault encrypt secrets.yml
# 4. Reference in playbooks/group_vars/host_vars as needed
