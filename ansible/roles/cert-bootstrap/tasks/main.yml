---
# Role: cert-bootstrap
# This role is a stub for later distribution of generated certs into secret stores
- name: Ensure cert cache directory exists
  file:
    path: "/var/cache/cluster-cert-bootstrap"
    state: directory
    mode: '0755'

- name: Placeholder - copy generated certs to control-plane hosts (NOT IMPLEMENTED)
  debug:
    msg: "This role will later handle distributing certs to hosts or uploading to secret stores."
